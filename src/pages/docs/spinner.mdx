import { Layout, Divider, InlineCode, Spinner, Container, Text } from "@zeal-ui/core";
import { CodeSnippet } from "../../components/index";
const spinnerImport=`
import { Spinner } from "@zeal-ui/core";
`;
const spinner=`
<Spinner />
`;
const spinnerCustomized=`
<Spinner width="5rem" height="5rem" color="blue"/>
`;

<Layout title="Spinner">

# Spinner

<Divider />

## Import 

<CodeSnippet code={spinnerImport}/>

## Usage

### Spinner

<Spinner />

<CodeSnippet code={spinner}/>

### Spinner Customized

<Spinner width="5rem" height="5rem" color="blue"/>

<CodeSnippet code={spinnerCustomized}/>

## Props

<InlineCode>width</InlineCode>

**Description**

It is used to set the width of the spinner.

**Values**

String with valid width.

<Divider/>

<InlineCode>height</InlineCode>

**Description**

It is used to set the height of the spinner.

**Values**

String with valid height.

<Divider/>

<InlineCode>color</InlineCode>

**Description**

It is used to set the color of the spinner.

**Values**

"gray" | "red" | "yellow" | "blue" | "orange" | "green" | "purple" | "pink"

<Divider/>

## Note

To learn more about customizing the components check **[customizing components](/docs/customizing-components)** in the docs for more info.

</Layout>