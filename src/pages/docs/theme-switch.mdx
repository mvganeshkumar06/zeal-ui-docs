import { Layout, Divider, InlineCode, ThemeSwitch } from "@zeal-ui/core";
import { CodeSnippet } from "../../components/index";
const themeSwitchImport = `
import { ThemeSwitch } from "@zeal-ui/core";
`;
const themeSwitch = `
<ThemeSwitch/>
`;

<Layout title="Theme Switch">

# Theme Switch

It is used to switch between the light and the dark themes.

<Divider />

## Import

<CodeSnippet code={themeSwitchImport} />

## Usage

<ThemeSwitch />

<CodeSnippet code={themeSwitch} />

</Layout>
