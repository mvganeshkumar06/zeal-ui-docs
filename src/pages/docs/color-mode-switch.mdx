import { CodeBlock } from '../../components';

# Color Mode Switch

-   It is used to switch between light and dark color mode.

---

## Import

<CodeBlock
    code={`
import { ColorModeSwitch } from '@zeal-ui/core';
	`}
/>

## Usage

<CodeBlock
    code={`
<ColorModeSwitch/>
	`}
    live
/>
